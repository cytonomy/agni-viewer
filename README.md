# agni-viewer

Static host for a single **password-encrypted** document. The file here is AES-256-GCM ciphertext — it is unreadable without the password (decryption happens in your browser). No plaintext content lives in this repo.
